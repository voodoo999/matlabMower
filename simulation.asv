R = 2;

xstart = [0 0 55];
xodo = [0 0 55];
v = [0.2 0.5 0];
astart = [0 0 0];

X =[];
Y =[];
Xkin = [];
Ykin = [];
Xod = [];
Yod = [];
Ximu = [];
Yimu = [];

for i = 0:+.1:25
    v(1,3) = i;
    temp=kinModell(xstart, v);
    temp2=odometrie(xodo, v);
    xstart=temp.';
    xodo=temp.';
    
    Xod=[Xod xodo(1,1)];
    Yod=[Yod yodo(1,2)];
    
    Xkin=[Xkin xstart(1,1)];
    Ykin=[Ykin xstart(1,2)];
    
    X=[X R*cos(i)];
    Y=[Y R*sin(i)];
    
    
end

plot(X,Y, Xkin, Ykin, Xod)